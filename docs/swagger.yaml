info:
  contact: {}
paths:
  /index:
    get:
      responses:
        "200":
          description: OK
          schema:
            type: string
      tags:
      - GetIndex
  /user/getUserList:
    get:
      responses:
        "200":
          description: OK
          schema:
            type: string
      tags:
      - 获取用户列表
swagger: "2.0"
